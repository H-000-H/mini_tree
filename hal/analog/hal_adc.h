/* SPDX-License-Identifier: Apache-2.0 */
/*
 * ADC HAL — 模数转换抽象接口
 *
 * 提供 8/10/12/14/16 位精度配置与通道初始化
 * 支持 read_raw / read_mv 两种读取及 force_stop 安全停机
 */
#ifndef HAL_ADC_H
#define HAL_ADC_H

#include <stdint.h>
#include <stddef.h>

#ifdef __cplusplus
extern "C"
{
#endif

                                                            /*ADC 精度枚举*/
/*===========================================================================================================================================================*/
typedef enum
{
    HAL_ADC_WIDTH_8BIT,
    HAL_ADC_WIDTH_10BIT,
    HAL_ADC_WIDTH_12BIT,
    HAL_ADC_WIDTH_14BIT,
    HAL_ADC_WIDTH_16BIT,
} hal_adc_width_t;
/*===========================================================================================================================================================*/

                                                            /*通道配置*/
/*===========================================================================================================================================================*/
struct hal_adc_config
{
    int             channel;    /* ADC 通道号 */
    hal_adc_width_t width;      /* 转换精度 */
    int             vref_mv;    /* 参考电压(mV), 0 = 使用硬件默认 */
};
/*===========================================================================================================================================================*/

                                                            /*ADC 实体与 API*/
/*===========================================================================================================================================================*/
struct hal_adc
{
    int (*init)(struct hal_adc* adc, const struct hal_adc_config* cfg);
    int (*read_raw)(struct hal_adc* adc, uint32_t* val);    /* 读取原始值 */
    int (*read_mv)(struct hal_adc* adc, uint32_t* mv);      /* 读取转换结果(mV) */
    int (*deinit)(struct hal_adc* adc);
    void* _impl;
};

void hal_adc_init_struct(struct hal_adc* adc);
void hal_adc_force_stop(void);
/*===========================================================================================================================================================*/

                                                            /*ADC ioctl 命令定义*/
/*===========================================================================================================================================================*/
#define ADC_CMD_READ_RAW 0x70
#define ADC_CMD_STOP     0x71

struct adc_read_arg
{
    int channel;
    int atten;          /* 参考电压衰减 */
    int bitwidth;
    int* out_raw;
};
/*===========================================================================================================================================================*/

#ifdef __cplusplus
}
#endif

#endif /* HAL_ADC_H */
