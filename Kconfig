# mini_tree Kconfig 入口 (非 ESP 路径)
# ESP 路径走 Kconfig.projbuild (由 ESP-IDF confgen 自动注入顶层树)。
# 公共配置树在 Kconfig.mini_tree, 两套入口各自 source 同一份, 避免分叉。

mainmenu "mini_tree Configuration"
source "Kconfig.mini_tree"
