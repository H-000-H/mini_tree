# ==============================================================================
# lib — 本机无需 RTOS 内核
#   FreeRTOS 由 ESP-IDF 的 REQUIRES freertos 提供，
#   RT-Thread 已移除，裸机模式使用 OSAL NULL 后端。
# ==============================================================================

add_library(lib INTERFACE)
